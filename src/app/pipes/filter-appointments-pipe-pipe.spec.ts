import { FilterAppointmentsPipePipe } from './filter-appointments-pipe-pipe';

describe('FilterAppointmentsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAppointmentsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
